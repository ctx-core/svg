import { markup_ } from './markup_';
export function _svg_preprocess(builder_opts = {}) {
    const markup = markup_(builder_opts);
    return {
        markup,
    };
}
//# sourceMappingURL=src/_svg_preprocess.js.map